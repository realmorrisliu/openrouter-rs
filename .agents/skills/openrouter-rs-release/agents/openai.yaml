interface:
  display_name: "OpenRouter-rs Release"
  short_description: "Prepare SDK and CLI releases safely"
  default_prompt: "Use $openrouter-rs-release to prepare, verify, and publish the next openrouter-rs or openrouter-cli release."
